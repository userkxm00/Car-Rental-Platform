# Enforce declared Node/npm engine requirements instead of silently running on
# unsupported runtimes (see "engines" in the root package.json).
engine-strict=true

# Keep install output focused; dependency funding banners add noise.
fund=false
